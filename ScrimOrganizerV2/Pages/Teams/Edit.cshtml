@page
@model ScrimOrganizerV2.Pages.Teams.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Team</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Team.ID" />
            <div class="form-group">
                <label asp-for="Team.Name" class="control-label"></label>
                <input asp-for="Team.Name" class="form-control" />
                <span asp-validation-for="Team.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>

        <!--<a asp-page="./Summoners/Create" asp-route-id="Team.ID">Add me daddy</a>
    <form method="post" action="/Summoners/Create">
        <div class="form-group">
            <input type="hidden" asp-for="Team.ID" />
            <input type="submit" value="Add Summoner" class="btn btn-primary" />
        </div>
    </form>
        -->
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
